{
  "name": "ibw-api-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Infinite Binary Wobble — Cloudflare Worker API (metering + payments trust root).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:init": "wrangler d1 execute ibw --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260610.1",
    "typescript": "~6.0.2",
    "vitest": "^4.1.8",
    "wrangler": "^4.99.0"
  }
}
