{
  "name": "infinite-binary-wobble",
  "private": true,
  "version": "0.4.0",
  "description": "A two-player gravitational binary-star game in the browser.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "export:fixtures": "node --experimental-strip-types scripts/export-golden-fixtures.mjs"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.0.16",
    "vitest": "^4.1.8"
  }
}
